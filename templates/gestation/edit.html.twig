{% extends 'AnimalManagement.html.twig' %}

{% block title %}Edit Gestation{% endblock %}

{% block body %}
    <section class="contact-area">
    <div class="container">
        <div class="row align-items-center justify-content-between">
            <div class="col-12 col-lg-5">
                <!-- Section Heading -->
                <div class="section-heading text-center">
                    <h2>Modifier Animal</h2>
                </div>
                    
                <!-- Form Area -->
                <div class="contact-form-area mb-100">
                {{ include('gestation/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('gestation/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="col-12 col-lg-6">
                <!-- Video Area -->
                <div class="alazea-video-area mb-100">
                    <img src="{{ asset('front/img/bg/07.jpg') }}" alt="">
                </div>
            </div>
            
        </div>
    </div>
    <a href="{{ path('app_gestation_index') }}" class="btn alazea-btn active">back to list</a>
{% endblock %}
