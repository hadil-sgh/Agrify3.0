<form method="post" action="{{ path('app_nutritional_value_needs_delete', {'id': nutritional_value_need.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutritional_value_need.id) }}">
    <button class="btn alazea-btn mt-15">Delete</button>
</form>
