{% extends 'base.html.twig' %}

{% block title %}TypedeReclamation{% endblock %}

{% block body %}
    <h1>TypedeReclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typede_reclamation.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ typede_reclamation.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typede_reclamation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typede_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_typede_reclamation_edit', {'id': typede_reclamation.id}) }}">edit</a>

    {{ include('typede_reclamation/_delete_form.html.twig') }}
{% endblock %}
