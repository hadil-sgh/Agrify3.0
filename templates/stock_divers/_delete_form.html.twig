<form method="post" action="{{ path('app_stock_divers_delete', {'id': stock_diver.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_diver.id) }}">
    <button class="btn">Supprimer</button>
</form>
