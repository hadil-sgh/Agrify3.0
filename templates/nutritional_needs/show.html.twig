{% extends 'base.html.twig' %}

{% block title %}NutritionalNeeds{% endblock %}

{% block body %}
    <h1>NutritionalNeeds</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nutritional_need.id }}</td>
            </tr>
            <tr>
                <th>SpeciesNutritionalNeeds</th>
                <td>{{ nutritional_need.speciesNutritionalNeeds }}</td>
            </tr>
            <tr>
                <th>ProductionStatus</th>
                <td>{{ nutritional_need.productionStatus }}</td>
            </tr>
            <tr>
                <th>Sex</th>
                <td>{{ nutritional_need.sex }}</td>
            </tr>
            <tr>
                <th>MinimumWeight</th>
                <td>{{ nutritional_need.minimumWeight }}</td>
            </tr>
            <tr>
                <th>MaximumWeight</th>
                <td>{{ nutritional_need.maximumWeight }}</td>
            </tr>
            <tr>
                <th>ProductionGoal</th>
                <td>{{ nutritional_need.productionGoal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nutritional_needs_index') }}">back to list</a>

    <a href="{{ path('app_nutritional_needs_edit', {'id': nutritional_need.id}) }}">edit</a>

    {{ include('nutritional_needs/_delete_form.html.twig') }}
{% endblock %}
