<form method="post" action="{{ path('app_nutritional_needs_delete', {'id': nutritional_need.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutritional_need.id) }}">
    <button class="btn">Delete</button>
</form>
