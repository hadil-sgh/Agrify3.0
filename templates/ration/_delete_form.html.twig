<form method="post" action="{{ path('app_ration_delete', {'id': ration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ration.id) }}">
    <button class="btn">Delete</button>
</form>
