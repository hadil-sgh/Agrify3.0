<form method="post" action="{{ path('app_nutrition_value_needs_delete', {'id': nutrition_value_need.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutrition_value_need.id) }}">
    <button class="btn">Delete</button>
</form>
