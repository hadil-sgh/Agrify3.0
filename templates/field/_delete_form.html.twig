<form method="post" action="{{ path('app_field_delete', {'field_id': field.field_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ field.field_id) }}">
    <button class="btn">Delete</button>
</form>
