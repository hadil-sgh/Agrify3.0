<form method="post" action="{{ path('app_AnimalBatch_delete', {'id': AnimalBatch.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ AnimalBatch.id) }}">
    <button class="btn">Delete</button>
</form>
