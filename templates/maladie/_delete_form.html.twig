<form method="post" action="{{ path('app_maladie_delete', {'id': maladie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maladie.id) }}">
    <button class="btn">Delete</button>
</form>
